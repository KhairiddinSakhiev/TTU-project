@using Domain.Entities
@model List<Slider>

<div class="row">
    <div class="col-md-12">
        <a asp-action="Create" asp-controller="Slider" class="btn btn-success">Добавить</a>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table">
            <thead>
            <tr>     
                    <td>Title</td>
                    <td>image</td> 
                    <td>Enabled</td>
                           
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    
                    <td>@item.Title</td>
                     <td><img width="100" height="100" src="@item.Image" alt="Photo" accept="application" /></td>
                    <td>@item.Enabled</td>
                     
                   
                   

                    <td>
                        <a asp-action="Edit" asp-controller="Slider" asp-route-id="@item.Id">
                            Измениить
                        </a>
                        <a asp-action="Delete" asp-controller="Slider" asp-route-id="@item.Id">
                            Удалить
                        </a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>